@model IEnumerable<Second_Project.Models.AspNetUserRole>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Role.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.UserName)
                </td>
                <td>
                    @*<a href="/AspNetUserRoles/Edit">Edit</a>*@
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.RoleId }) |*@
                    @*@Html.ActionLink("Details", "Details", new { id = item.UserId, item.RoleId }) |*@
                    @Html.ActionLink("Delete", "Delete", new { id = item.UserId, item.RoleId })
                </td>
            </tr>
        }
    </tbody>
</table>
